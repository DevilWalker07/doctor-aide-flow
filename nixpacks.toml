providers = ["node"]

[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["...", "nodejs_22"]

[phases.install]
dependsOn = ["setup"]
cmds = ["npm ci"]

[phases.build]
dependsOn = ["install"]
cmds = ["npm run build"]

[start]
cmd = "npm start"
